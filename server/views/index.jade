doctype html
html
  head
    title= title
    meta(content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no', name='viewport')
    // Load a resource from behind the proxy with the ticket appended.
    // In this case requirejs which will load main.js when done loading.
    script(data-main='main.js' src='https://localhost/resources/assets/external/requirejs/require.js?qlikTicket=' + ticket)
  body